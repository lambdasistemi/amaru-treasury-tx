---
sha: 4566427de2452b6f08be343bed3d706a73e2981a
patch: feat-006-withdraw-zero-noop
task: T030
---

## ready

## approved

T030 wires the runner to the typed `WithdrawResult`: zero-rewards traces `WweNoRewards account` and returns without `BSL.putStr`/`BSL.writeFile`, satisfying FR-004 (`--out` neither created nor modified, exit 0). The `WweValidityComputed`/`WweIntentReady` trace events stay on the positive-rewards branch where they belong; translation failures still go through `abortWithdraw`. End-to-end runner coverage is the explicit T031/T032 next step.
