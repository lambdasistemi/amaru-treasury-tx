---
sha: ead7224e7b9adca9632ce4b13bbff7655ef25819
task: T023
patch: wip-withdraw-cli-runner
---

## ready

## changes-requested

- commit message — title: rename to `feat(006): withdraw-wizard CLI runner`. `wip:` is not conventional-commits, and embedding the task id in the title (`T023:`) is non-standard. Keep T023 in the body or in `task:` frontmatter, not in the title.
- commit message — body: this patch is title-only. Add a body via `stg edit -m "<title>" -m "<body>"`. Suggested body:

  > Adds the @withdraw-wizard@ subcommand parser and runner mirroring
  > @swap-wizard@: typed answers, registry verification, resolver,
  > pure translation, and JSON-only stdout/`--out`. Provider hook for
  > the live stake-reward query stubs out — see `<issue link>` — so
  > the runner currently aborts on any real invocation; the pure
  > translation and the resolver test harness already exercise the
  > positive-rewards path. T023 stays open until the live query lands.

- `app/amaru-treasury-tx/Main.hs:1062` — `wreQueryRewardsLovelace` aborts unconditionally because the upstream `Provider` lacks a stake-reward query. That makes the CLI runner non-functional for User Story 1's acceptance scenario (a real run never produces an `intent.json`). Per `[Drops need issues]`, this scope drop must:
  1. Have a GitHub issue filed against `lambdasistemi/amaru-treasury-tx` (or upstream `cardano-node-clients` if that's where the missing method belongs) tracking the live reward query.
  2. Reference that issue number in the `abortWithdraw` message and in `specs/006-withdraw-wizard/contracts/withdraw-wizard-cli.md` so operators learn the limitation from the trace, not from reading the source.
  3. Either flip `T023` back to `[ ]` in `tasks.md` with a note "shipped runner skeleton; live reward query tracked in #N", or split T023 into T023a (skeleton) and T023b (live query) and check only T023a.

  Either path is acceptable; pick the lighter one. Without (1) the gap is invisible the moment this lands.

- `app/amaru-treasury-tx/Main.hs:980` — `error "withdraw-wizard: missing scope in RegistryView (post-verify)"` raises a partial `error`. The post-verify invariant is supposed to make this impossible, but the rest of the runner uses `abortWithdraw` for typed failures with exit 3 + trace event. Prefer `abortWithdraw tr "internal: missing scope in RegistryView (post-verify); please file a bug"` so an unexpected state still emits the typed `WweAborted` line that operators are scripted against, instead of dying without a trace.
