---
sha: b844296dd9f2bdf453856cc8a69f4ae9fbf52021
patch: test-006-release-withdraw-help-smoke
task: T048
---

## ready

## approved

T048 extends release-path smoke coverage to `withdraw-wizard --help`:

- `nix/checks.nix` adds the 5-needle check + 10s timing budget alongside swap-wizard and tx-build.
- `nix/linux-artifact-smoke.nix` adds the same 5 needles, plus a refactor that now times all three help commands instead of only swap-wizard.
- `flake.nix` Homebrew test stanza calls the subcommand.
- `.github/workflows/darwin-release.yml` invokes `withdraw-wizard --help` in three release paths (homebrew tap, lambdasistemi tap, dev tap).

Body matches dictation. T023a/T023b shipped the CLI surface long ago, so artifacts on this branch will have the subcommand.

Soft, non-blocking: the five needle strings are now duplicated in three places (`scripts/smoke/withdraw-wizard-zero-rewards`, `nix/checks.nix`, `nix/linux-artifact-smoke.nix`). Same drift risk as the existing swap-wizard pattern. Worth a follow-up for shared needle constants if a flag rename costs three places to update.
