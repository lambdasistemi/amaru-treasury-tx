---
sha: 8f1cee9293e027b2314fa7b3faa7423eee03b09a
patch: test-006-confirm-zero-smoke
task: T032
---

## ready

## approved

Single-line tasks.md tick for T032. The actual verification (just smoke green with the wired withdraw zero-rewards guard) is covered by `gate.sh` — trust contract applies. `test(006):` reads as a stretch for a pure checkbox tick (chore(006): would also fit), but the body unambiguously frames it as the T032 attestation, so it's not worth a rename round.
