---
sha: e30fa383fadba1d9492aff9546d33fe66cd34088
patch: build-006-include-withdraw-cbor-fixtures
task: T041
---

## ready

## approved

T041 adds `test/fixtures/withdraw/**/*.cbor` to `extra-source-files` directly above the existing `*.json` glob, preserving alphabetical order (`.c` before `.j`). Now that T040's `expected.cbor` exists, the cabal glob no longer matches an empty set so the package stays Hackage-clean. Source distributions will carry the recorded golden alongside the harness.
