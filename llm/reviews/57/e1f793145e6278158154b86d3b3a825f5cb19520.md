---
sha: e1f793145e6278158154b86d3b3a825f5cb19520
patch: wip-withdraw-live-rewards
task: T023b
---

## ready

## approved

Both prior findings addressed: `StakeRewardsError`/`StakeRewardsEraMismatch` is the new typed return on `queryStakeRewardsLovelace`, and the caller (`Main.hs:1082`) translates `Left` to `abortWithdraw` so era-mismatch flows through the typed `WweAborted` trace. The pure helper `stakeRewardLovelaceFromRewards` is extracted and exercised by two unit tests covering the positive path and the "missing row → 0" semantics that US2 will depend on. Body and `Closes #58` retained.
