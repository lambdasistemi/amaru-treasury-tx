# Code review: b9106a5d1641601536604dae409851708165516b

decision: Approved

slice: T013-T020, production registry initiator extraction

## Scope Review

- Changed only the owned implementation files:
  `lib/Amaru/Treasury/Devnet/RegistryInit.hs`,
  `amaru-treasury-tx.cabal`,
  `test/unit/Amaru/Treasury/Devnet/RegistryInitSpec.hs`, and
  `test/devnet/Amaru/Treasury/Devnet/SmokeSpec.hs`.
- Did not edit specs, docs, README, PR metadata, issue metadata, or
  unrelated source.
- Moved reusable registry script derivation, NFT/reference-script
  publication, anchor rendering, and withdraw registry projection under
  `lib/`.
- Left `SmokeSpec.hs` as DevNet orchestration and verification for this
  slice. Swap-readiness helper code remains local to smoke and is not
  part of the registry-init extraction.

## Verification

- `git diff --check` passed.
- `nix develop --quiet -c cabal build lib:amaru-treasury-tx -O0`
  passed.
- `nix develop --quiet -c just unit "Amaru.Treasury.Devnet.RegistryInit"`
  passed with 3 examples and 0 failures.
- `nix develop --quiet -c cabal build test:devnet-tests -O0` passed.
- `nix develop --quiet -c fourmolu -m check lib/Amaru/Treasury/Devnet/RegistryInit.hs test/unit/Amaru/Treasury/Devnet/RegistryInitSpec.hs test/devnet/Amaru/Treasury/Devnet/SmokeSpec.hs` passed.
- `nix develop --quiet -c cabal-fmt -c amaru-treasury-tx.cabal`
  passed.
- `scripts/smoke/devnet-local --phase registry-init --run-dir /tmp/amaru-registry-init-red-review` still exits 64 with `devnet-smoke: unknown phase: registry-init`, which is the expected pre-T021/T022 RED boundary.
- `/code/llm-settings/shared/skills/pr/references/commit-gate.sh b9106a5d1641601536604dae409851708165516b` passed.

## Findings

- No blocking findings for T013-T020.
- Follow-up for the next slice: the new module currently exposes
  withdraw-oriented artifact helpers because the extracted live caller is
  withdrawal setup. T021-T027 must add registry-init phase artifacts and
  summary/provenance names without reintroducing construction logic into
  `SmokeSpec.hs`.
