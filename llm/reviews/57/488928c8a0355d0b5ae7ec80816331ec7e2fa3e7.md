---
sha: 488928c8a0355d0b5ae7ec80816331ec7e2fa3e7
patch: wip-withdraw-resolver-proof
---

## ready

## changes-requested

- commit message — title: keep `feat(006): resolve withdraw wizard environment`.
- commit message — body: this patch is title-only. Add a body via `stg edit -m "<title>" -m "<body>"`. Suggested body:

  > Adds `resolveWithdrawEnv` plus the `WithdrawWizardEvent` trace
  > surface (T018, T021, T022). The reward account is derived from
  > the selected scope's treasury script hash; reward amount and
  > chain tip come from a stub-testable effect record. CLI runner
  > (T023) is the next slice.

- `lib/Amaru/Treasury/Tx/WithdrawWizard.hs:402` — `withdrawNetworkConstants` hardcodes `""`/`0` for `ncSwapOrderAddress`, `ncUsdmPolicy`, `ncUsdmToken`, `ncSundaeProtocolFeeLovelace`, `ncExtraPerChunkLovelace`, `ncDefaultPoolId` just to fit the shared `NetworkConstants` shape. Withdraw only needs `ncSlotsPerHour`. Either (a) split the slot-conversion piece into its own type, (b) make the swap-only fields optional in `NetworkConstants`, or (c) populate the constants from the same source swap/disburse use so the resolver doesn't carry placeholder dead-state. Pick the lightest option that doesn't reintroduce swap coupling.
