# Work Review

sha: 663c0e2fd57d54a2cf2c8c669b31f209ada26451
slice: T035-T042, shipped DevNet registry-init command
decision: Approved

## Scope Review

- Adds the required nested command:
  `amaru-treasury-tx --network devnet --node-socket <socket> devnet registry-init`.
- Command requires explicit `--funding-address`, `--signing-key-file`,
  and `--run-dir`.
- Command rejects non-DevNet networks before reading signing-key
  material or opening the node socket.
- Command runner is a thin wrapper around
  `Amaru.Treasury.Devnet.RegistryInit`; transaction construction remains
  in production registry-init code.
- DevNet smoke now proves the shipped command runner path instead of a
  smoke-only publication path.
- No #148 staking, #149 governance/withdrawal, #150 disburse, swap, or
  external-role behavior added.

## Correction Applied

The first returned slice printed smoke-style keys under the
`registry-init:` prefix, for example
`registry-init: registry-init-seed-split-tx-id`. That contradicted
`contracts/devnet-registry-init.md`. The same subagent amended the
commit to print the command contract fields:

- `registry-init: seed-split-tx-id`
- `registry-init: registry-mint-tx-id`
- `registry-init: reference-scripts-tx-id`
- `registry-init: summary`
- `registry-init: registry`

Focused unit coverage now locks that renderer.

## Local Verification

- `git diff --check` -> pass.
- `nix develop --quiet -c just unit "registry-init command"` -> 3
  examples, 0 failures.
- `nix develop --quiet -c cabal build exe:amaru-treasury-tx -O0` ->
  pass.
- `nix develop --quiet -c cabal build test:devnet-tests -O0` -> pass.
- Focused `fourmolu -m check` on touched Haskell files -> pass.
- `nix develop --quiet -c cabal-fmt -c amaru-treasury-tx.cabal` ->
  pass.
- `nix develop --quiet -c just devnet-smoke registry-init` -> pass, 2
  examples, 0 failures, run `runs/devnet/20260516T193404Z`.
- HLint amend: Dalton reduced `registryInitLines` after the final gate
  found an eta-reduction warning; `nix develop --quiet -c just hlint`
  reported `No hints`.

## Live Evidence

- Seed split tx:
  `82b1f12f0ceeae86c50753a61528599c4d7b8ccef769a56accd3011c0e24084d`.
- Registry mint tx:
  `1f427e73979ee6150e69944fb384cbe0809148e64307a2a75221bacea8cb4ff9`.
- Reference scripts tx:
  `5c3227fe8511632669b5383246e7ff92ccc2add2988ee90ac1a24ecda6a10a44`.
- Scopes anchor:
  `1f427e73979ee6150e69944fb384cbe0809148e64307a2a75221bacea8cb4ff9#0`.
- Registry anchor:
  `1f427e73979ee6150e69944fb384cbe0809148e64307a2a75221bacea8cb4ff9#1`.
- Permissions reference:
  `5c3227fe8511632669b5383246e7ff92ccc2add2988ee90ac1a24ecda6a10a44#0`.
- Treasury reference:
  `5c3227fe8511632669b5383246e7ff92ccc2add2988ee90ac1a24ecda6a10a44#1`.

## Finalization Follow-Up

T043-T044 are orchestrator-owned and recorded in `tasks.md`: README,
docs, contracts, quickstart, review state, and PR metadata were aligned
after this code slice, and the full local gate passed before returning
the PR to external review.
