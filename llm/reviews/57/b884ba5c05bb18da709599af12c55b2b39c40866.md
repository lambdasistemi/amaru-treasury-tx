---
sha: b884ba5c05bb18da709599af12c55b2b39c40866
patch: feat-006-typed-withdraw-zero
task: T029
---

## ready

## changes-requested

- `lib/Amaru/Treasury/Tx/WithdrawWizard.hs:165` — `WithdrawNoRewards !Text !Integer` carries an `Integer` that is always `0`. The only construction site (`withdrawToTreasuryResult` at line 188) is the `EQ` branch of `compare ... 0`, so the field cannot legitimately be anything else. Two ways to land cleanly:
  1. Drop the `Integer` field: `WithdrawNoRewards !Text`. The trace event `WweNoRewards account lovelace` can either lose its `Integer` argument or hardcode `0` in the renderer — same observable trace text.
  2. Keep the field only if you have a near-term use for non-zero "no-op" cases (e.g. a future "below dust threshold" path that's also a no-op). If so, add a one-line Haddock note explaining the forward-looking shape so it doesn't read as accidental dead state.

  Per the project's "no features beyond what the task requires" rule, option 1 is the lighter path unless you already know option 2 is coming in the next slice.

- `lib/Amaru/Treasury/Tx/WithdrawWizard.hs:225` — `validateStatic` reads as "static analysis", not "everything except the reward sign". Consider renaming to `validateAnswersAndScope` (descriptive) or `validateNonReward` (explicit), or keep `validate` and add a one-line Haddock saying the reward sign is handled separately by `withdrawToTreasuryResult`. Non-blocking but the rename here is the moment to spend the bytes on clarity.

Substance is otherwise good: clean three-way `compare`, the existing `withdrawToTreasuryIntent` wrapper preserves back-compat with `Left WithdrawRewardsNotPositive` for callers that don't yet know about the new result type, and both new branches have unit coverage (translator output and renderer text).
