---
sha: 7aeaf9b87f3edbe26ae6d249c328de91ad52f4b1
patch: feat-006-typed-withdraw-zero
task: T029
---

## ready

## approved

Both prior findings addressed: `WithdrawNoRewards` now takes only `Text` (the always-zero Integer is gone, the renderer hardcodes `lovelace=0` so the operator-visible trace string is unchanged), and `validateStatic` is renamed to `validateAnswersAndScope` — descriptive about what it checks vs. what it deliberately omits (the rewards sign, handled by `withdrawToTreasuryResult`'s `compare`). Tests updated for the new constructor shape.
