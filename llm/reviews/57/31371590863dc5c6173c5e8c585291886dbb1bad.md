---
sha: 31371590863dc5c6173c5e8c585291886dbb1bad
patch: wip-t027-zero-rewards-no
task: T027
---

## ready

## changes-requested

- commit message — title: rename to `test(006): zero-rewards no-output unit test`. `wip:` is not conventional-commits and embedding the task id in the title (`T027:`) is non-standard — keep T027 in the `task:` frontmatter.
- commit message — body: title-only. Add via `stg edit -m "<title>" -m "<body>"`. Suggested:

  > Loads `test/fixtures/withdraw/zero-rewards/env.json` through the
  > `WithdrawEnv` parser and confirms the pure translator rejects with
  > `WithdrawRewardsNotPositive`. Also asserts that no `intent.json` is
  > present beside the fixture, guarding against future fixture drift.
  > T028 (CLI smoke) still pending.

Substance is fine: the test adds the fixture-load path that the inline `(env{weRewardsLovelace = 0})` rejection test in `goldenCase` did not cover, and the `doesFileExist` assertion documents the "no intent.json in zero-rewards/" invariant.
