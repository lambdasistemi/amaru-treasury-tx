---
sha: 5671677539b1bc612d97e245ae0cea0f9a73ab0a
patch: feat-006-wire-withdraw-build
task: T037
---

## ready

## approved

Dispatcher wire-up lands cleanly: `SWithdraw` now routes to `runWithdraw` instead of throwing in `runBuild`, and `runWithdraw` is exported as a stub that fails loudly until T038 fills the body. Module Haddock updated accordingly (withdraw no longer points at #45, just reorganize → #46). Bisect-safe: behavior is still "throws userError" at this commit; T035's golden stays `pendingWith` because expected.cbor isn't there yet, so no test regresses.

Soft, non-blocking: the stub's error string `runWithdraw: not yet implemented (T038)` references an internal task id with no context. Since T038 lands inside this same PR the stub never ships to main, but if you happen to land on this sha during a bisect the error reads as opaque. A small tweak — e.g. `runWithdraw: not yet implemented for this build` — would be friendlier without burning a round.
