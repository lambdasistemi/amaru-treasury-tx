---
sha: 41de3f863b0ad28729a9068d5965ac717bcb09d9
patch: test-006-withdraw-golden-red
task: T035
---

## ready

## approved

Golden harness lands T035 cleanly: decodes the unified withdraw intent via `decodeTreasuryIntentFile`, loads the frozen ChainContext via `readSwapFixture`/`toFrozenContext`, runs `runFromIntent`, compares hex-encoded actual against the on-disk expected. The `unless expectedExists $ pendingWith` short-circuit keeps the test in `pending` until T040 records `expected.cbor` — the right state for a RED golden whose fixture is the next slice. Placeholder `1 + 1 == 2` is replaced.

Soft, non-blocking: `readSwapFixture` is named after its swap ancestor but reads any frozen context. Worth a future rename to `readFrozenContextFixture` in `Amaru.Treasury.ChainContext.Fixture` so the withdraw/disburse callers stop reading like cross-action coupling. Not this patch's job.
