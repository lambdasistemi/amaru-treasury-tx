---
sha: cad0c0e4bf96087bf8a469699b06ff138db5ce06
patch: feat-006-implement-withdraw-runner
task: T038
---

## ready

## changes-requested

- commit message — title: rename to `feat(006): implement withdraw runner`. `wip:` is not conventional-commits and embedding the task id in the title (`T038:`) is non-standard — keep T038 in the `task:` frontmatter. This is the same recurring pattern flagged on code-2/code-4/code-9/code-13 etc.
- commit message — body: title-only. Add via `stg edit -m "<title>" -m "<body>"`. Suggested:

  > Fills in `runWithdraw` end-to-end: required-UTxO precondition,
  > `withdrawProgram` + label-1694 metadata via the operational monad,
  > build, `alignCardanoCliBuildFee`, re-evaluation summary, Conway
  > CBOR. The unit test in `TreasuryBuildSpec` exercises the
  > missing-UTxO precondition; the happy path is exercised by the
  > T035 golden once T040 records `expected.cbor`.

- `lib/Amaru/Treasury/TreasuryBuild.hs:266` — `alignCardanoCliBuildFee pp refUtxos 1 tx0` passes `1` as a magic number. Without a comment or a named binding, the next reader has to grep `alignCardanoCliBuildFee` to learn that `1` is the expected signer count (or whatever it actually represents). If this number matches `runSwap`/`runDisburse` for the same reason, lift it to a shared `oneSignerCount = 1` constant in the module (or note in a Haddock line). If withdraw genuinely needs a different value than the siblings, justify it in a one-line comment so a future maintainer doesn't quietly normalize it during refactoring.

Substance is otherwise solid: the missing-UTxO check is correctly ordered before any pparams use, error paths are typed via `userError`, the re-evaluation populates `tbrScriptResults`, and the runner mirrors `runSwap`/`runDisburse` shape so the dispatcher stays uniform.
