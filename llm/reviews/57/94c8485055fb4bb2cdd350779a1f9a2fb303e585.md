---
sha: 94c8485055fb4bb2cdd350779a1f9a2fb303e585
patch: wip-withdraw-cli-runner
task: T023a
---

## ready

## approved

All four prior findings addressed: conventional title, body matching the dictated text plus a closing line on T023a/T023b, and issue #58 tracking the live stake-reward query is referenced in `Main.hs:1059`, the `abortWithdraw` message at `Main.hs:1074`, `contracts/withdraw-wizard-cli.md`, and `tasks.md` (split into T023a `[x]` / T023b `[ ]`). The `error` in `traceWithdrawRegistryView` is now `abortWithdraw` so unexpected post-verify state still emits the typed `WweAborted` trace event. Issue #58 verified open at `lambdasistemi/amaru-treasury-tx#58`.
