---
sha: 9d69ac5ab53a5328bfcef498e2e6f506c9afb81e
patch: feat-006-implement-withdraw
---

## ready

## approved

Phase 2 contract is complete and consistent: `WithdrawInputs` carries the two payload fields, the schema asset is regenerated with `minimum: 1` on `rewardsLovelace` and a `hex28` ref, the network-aware `parseRewardAccountForNetwork` correctly maps `mainnet`→`Mainnet` and `preprod`/`preview`→`Testnet` (rejecting unknown networks), and `translateWithdraw` validates positive rewards before mapping to the existing `WithdrawIntent`. Test coverage hits every FR-008 mismatch case (withdraw+swap, withdraw+disburse, withdraw+reorganize) plus mainnet/preprod network parity and the non-positive guard.

Minor non-blocking note: data-model.md §1 names the payload fields `wiTreasuryRewardAccount`/`wiRewardsLovelace`; the implementation uses `wdi*` to avoid clashing with `WithdrawIntent`'s own `wi*` prefix. The disambiguation is sensible — consider refreshing data-model.md to match in a later docs touch-up.
