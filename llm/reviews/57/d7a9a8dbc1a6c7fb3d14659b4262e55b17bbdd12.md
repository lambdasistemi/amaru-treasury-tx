---
sha: d7a9a8dbc1a6c7fb3d14659b4262e55b17bbdd12
patch: wip-t026-zero-rewards-fixture
task: T026
---

## ready

## changes-requested

- commit message — title: rename to `test(006): zero rewards fixture`. `wip:` is not conventional-commits and embedding the task id in the title (`T026:`) is non-standard — keep T026 in the `task:` frontmatter. `test:` is the right type for adding a test fixture; `chore(006):` is also acceptable.
- commit message — body: title-only. Add via `stg edit -m "<title>" -m "<body>"`. Suggested:

  > Adds the zero-rewards resolver fixture used by T027/T028. Mirrors
  > `synthetic/env.json` but with `rewardsLovelace = 0` so the runner
  > can exercise the "nothing to withdraw" no-op without invoking the
  > live LSQ query.
