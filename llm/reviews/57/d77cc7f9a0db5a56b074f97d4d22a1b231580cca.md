---
sha: d77cc7f9a0db5a56b074f97d4d22a1b231580cca
patch: test-006-withdraw-pipe-smoke
task: T046
---

## ready

## approved

T046 lands the offline pipe smoke: fixture sanity, schema validation, resolver pass, golden replay. Each subordinate cabal test references an existing approved test name (a31de1d resolver, 8eec3bc schema, 41de3f8 + 10b29d5 golden). Body explains the deliberate offline-by-fixture design — the live CLI needs a node socket so CI exercises the contract through deterministic state.

Soft, non-blocking: this smoke does not invoke the `amaru-treasury-tx withdraw-wizard` binary at all, unlike `withdraw-wizard-zero-rewards` (T028) which does an `--help` surface check. FR-010 stream separation (stdout JSON, stderr traces) is consequently only validated indirectly via the trace-text grep on the fixture. Consistent with the project's CI constraint, but if a later regression slips past unit/golden coverage and only manifests at the binary boundary, this won't catch it.
