---
sha: 10b29d51555825d32fec592215a36e76165efa48
patch: test-006-withdraw-expected-cbor
task: T040
---

## ready

## approved

T040 lands `expected.cbor` (2028 hex bytes ≈ 1014 CBOR bytes) plus the explicit UPDATE_GOLDENS regeneration flow. The harness now actively asserts byte equality once the file exists, falls back to `pendingWith` with an operator-friendly message if it's missing, and the regenerate path is gated by `UPDATE_GOLDENS=1`. CBOR header `84ab00d90102…` parses as a Conway tx-of-4 with an 11-field body map (plausible for a withdrawal with treasury output, fee, ttl, reference inputs, withdrawals, metadata). Determinism comes from the frozen pparams/utxos/exunits already approved in T033 — T042 will exercise the assertion path end-to-end.

Soft, non-blocking: the `UPDATE_GOLDENS=1` branch writes the file but does not assert. A nervous CI environment that accidentally exports the flag would silently regenerate. UPDATE_GOLDENS isn't a CI variable, and the pending wording surfaces the flag, so this is acceptable; same as the existing swap/disburse golden pattern.
