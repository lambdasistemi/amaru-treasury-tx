---
sha: 95627bb50fd124098164a57fabea05a67cf6bf20
patch: test-006-wire-withdraw-zero-smoke
task: T031
---

## ready

## approved

T031 wires `scripts/smoke/withdraw-wizard-zero-rewards` into `just smoke` and trims the now-stale "deferred until T031" comment in the script header.

Soft observation, not blocking and not actionable here: the smoke as-shipped covers help-surface + fixture-invariant + the T027 unit test by `--match`, but does not invoke the runner end-to-end against the zero-rewards env so the contract's "exit 0, no stdout, no --out write" behaviors rely on T030's runner case-split + T027's pure-translator test rather than an integration check. Consistent with the existing smoke pattern (`swap-wizard-signers`, `tx-build-pipe`); call it out if a later regression slips past the unit/case-split layer and you decide to invest in stub-Provider-driven smoke runs.
