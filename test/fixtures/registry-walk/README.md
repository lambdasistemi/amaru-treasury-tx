# Registry Walk Fixture

`metadata.json` is copied from:

`pragma-org/amaru-treasury@99600d8cedf0e3c4894fe7f45d5e8abad2289d76`

Path:

`journal/2026/metadata.json`

The fixture is an untrusted hint in tests. The verifier tests build
independent chain-anchor data and then mutate both sides to prove that
metadata fields are checked individually.
