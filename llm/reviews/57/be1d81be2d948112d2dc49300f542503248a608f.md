---
sha: be1d81be2d948112d2dc49300f542503248a608f
patch: wip-withdraw-wizard
---

## ready

## approved

Phase 1 scaffolding: cabal exposes the new modules, test stanzas list the new specs, fixture dirs use `.gitkeep`, justfile documents the `just golden withdraw` selector, and the empty-export skeletons compile. Title now matches conventional-commits (`feat(006):`). Diff vs the previous sha is empty — only the message changed. Bisect-safe.
